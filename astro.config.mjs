// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // 以后部署到 GitHub Pages / Cloudflare Pages 时，把 site 改成你的正式网址
  site: 'https://eleanorliu737.github.io',
});
